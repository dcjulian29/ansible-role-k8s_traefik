---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: dashboard-basicauth
  namespace: "{{ traefik_namespace }}"
spec:
  basicAuth:
    secret: "{{ traefik_namespace }}-dashboard-auth"
